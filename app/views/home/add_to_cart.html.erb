
<% @all_cart.each do |cart| %>
  <% pp = cart.product_id %>
  <% all = Product.find(pp) %>
   	
  <%= all.name %><br>
  <%= all.price %><br>
  <%= image_tag all.images.first.thumb.url.to_s %>
<% end %>
